<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.green.smart_grade.admin.board.BoardMapper">

    <insert id="insBoard" useGeneratedKeys="true" keyProperty="iboard">
        INSERT INTO board
        (iadmin, title, ctnt, importance)
        VALUES
        (#{iadmin},#{title}, #{ctnt}, #{importance})
    </insert>

    <update id="updBoard">
        UPDATE board
        SET ctnt = #{ctnt},
        title = #{title},
        importance = #{importance}
        WHERE iboard = #{iboard}
    </update>

    <select id="selBoard">
        SELECT iadmin, title,importance, created_at createdAt
        FROM board
        WHERE importance = 0
        ORDER BY createdAt DESC
    </select>

    <select id="selBoardImportance">
        SELECT iadmin, title, importance, created_at createdAt
        FROM board
        WHERE importance = 1
        ORDER BY createdAt DESC
    </select>


    <!--    <select id="selByIdBoard">-->
    <!--        SELECT-->
    <!--        FROM board-->
    <!--        WHERE iboard = #{iboard}-->
    <!--    </select>-->


<!--    <update id="updDelYnBoard">-->
<!--        UPDATE board-->
<!--        SET-->
<!--        WHERE-->
<!--    </update>-->

</mapper>